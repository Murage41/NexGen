# Usually blank in production because the mobile app is served by the backend
# and will call the same origin at /api.
# Android/Capacitor builds can instead set the station URL from the login screen.
VITE_API_URL=
